let hello = "Hello";
let world = "World";
let epicPhrase = hello + " " + world + "!";
console.log(epicPhrase);

// TEMPLATE LITERALS.
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

// MULTI-LINES.
let string = "Esto es una línea. \n " + "Esto es otra línea.";
console.log(string);

let string2 = `Esto es una línea.
 Esto es otra línea.`;
console.log(string2);